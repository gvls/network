## 2 `信道channel` 
Meaning : media of transport information of one direction

### 3  method of transport information
#### 4   `单工通信` / `单向通信` 
such as : `广播` 

#### 4   `半双工通信` / `双向交替通信` 
have two `channel` 

#### 4   `全双工通信` / `双向同时通信` 
have two `channel` 
